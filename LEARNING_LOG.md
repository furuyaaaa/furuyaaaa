# 学習ログ

新しい日付を **上に** 足していきます。

---

## テンプレート（コピーして使う）

```markdown
## YYYY-MM-DD

- **やったこと**:
- **理解したこと**:
- **次やること**（任意）:
```

---

## 2026-07-14

### 1. 詳細設計全体の復習

- タスク管理システムの詳細設計全体を復習した。
- Spring Boot の構成要素として、`Controller`、`Service`、`Repository`、`DTO`、`Entity` の役割を整理した。
- SQL、シーケンス図、データベース連携の流れを確認した。

### 2. 詳細設計書作成

- Controller 設計を整理した。
- Service 設計を整理した。
- Repository 設計を整理した。
- JPQL を利用した検索メソッド（`@Query`）の設計を確認した。
- DTO 設計を整理した。
- Entity 設計を整理した。
- シーケンス図と SQL 設計を確認した。
- 詳細設計書に **記載する内容** と **記載しない内容** の線引きを整理した。

### 3. Spring Data JPA と JPQL

- Spring Data JPA の役割を復習した。
- `@Query` を使った JPQL の考え方を確認した。
- Repository が DB 操作の窓口になり、Entity とテーブルのマッピングを通じてデータを扱う流れを整理した。

### 4. DTO 設計

- DTO の役割を整理した。
- DTO は画面・API の入出力に使うデータの形を表すものとして理解した。
- Laravel でいう `Resource` や `Request` との違いを確認した。
- Spring Boot では DTO、Entity、Request / Response 相当の責務を分けて考えることが重要だと整理した。

### 5. Laravel と Spring Boot の比較

- Laravel と Spring Boot の構成を比較した。
- Laravel の経験を Spring Boot の `Controller` / `Service` / `Repository` / `Entity` / `DTO` に置き換えて理解した。
- Java の現在のスキルレベルを整理し、今後伸ばすべき領域を確認した。

### 6. Laravel ルーティング

- `Route::get()` の書き方を確認した。
- Controller へのルーティング方法を整理した。
- `name()` によるルート名設定を確認した。
- URL、Controller のメソッド、ルート名のそれぞれの違いを整理した。

### 気づき

- レイヤー構造を理解すると、各クラスの責務が見えやすくなる。
- Spring Boot では、Controller が入口、Service が業務ロジック、Repository が DB 操作、Entity がテーブル対応、DTO が入出力データの形を担う。
- 詳細設計書を作ることで、実装前に処理の流れ・責務・データの受け渡しを整理できる。
- Laravel のルーティングと Spring Boot の Controller 設計を比較すると、「リクエストをどこに渡すか」という共通構造が見えやすい。

## 2026-07-13

### システム開発

- タスク管理システムの詳細設計全体を確認した。
- Repository、Entity、DTO、Service の役割を整理した。
- 業務システムで使われる設計書の流れを確認した。

### 振り返り

- Laravel で学んだ構造を Spring Boot に置き換えて理解することを意識した。
- 既に知っている概念と新しい技術を対応させることで、Spring Boot の構成が掴みやすくなった。

## 2026-07-10

### 1. システム開発

- タスク管理システムの詳細設計を開始した。
- Controller 設計の考え方を学習した。
- `getTasks` が複数形で、それ以外のメソッドが単数形になっている理由を確認した。
- 一覧取得は複数件を返すため `getTasks`、詳細取得・作成・更新・削除は基本的に単一リソースを扱うため単数形になる、と整理した。

### 2. Spring Boot / ORM

- Spring Boot における ORM（Spring Data JPA）の役割を理解した。
- Entity とテーブルがマッピングされ、Repository を通じて DB 操作を行う流れを確認した。
- Spring Data JPA により、SQL を直接すべて書かなくても基本的な CRUD 操作を扱えることを整理した。

### 3. Laravel との比較

- `Repository ≒ Laravel の Model` という考え方を確認した。
- Entity と Model の違いを整理した。
- Laravel では Model が ORM の中心になりやすい一方、Spring Boot では Entity と Repository に責務が分かれると理解した。
- テーブルとのマッピング（ORM）の仕組みを、Laravel 経験と比較しながら確認した。

### 気づき

- Spring Boot は Laravel よりもクラスの責務が細かく分かれているため、最初に Controller / Service / Repository / Entity / DTO の役割を押さえることが重要。
- 命名の単数・複数には、扱うデータが 1 件か複数件かという設計意図が表れる。

## 2026-07-09

### 1. 要件定義書の最終調整

- システム開発要件定義書の最終調整を行った。
- 機能要件、非機能要件、画面一覧、データ要件、制約事項、スケジュールを整理した。
- 学習内容を振り返りながら、システム開発全体の流れを確認した。

### 2. Excel・Word ツール

- ガントチャートテンプレートを作成・改良した。
- 実績行の追加、マイルストーン対応、進捗管理の仕組みを整えた。
- Word の目次、章番号、ページ削除など、要件定義書作成に必要な機能を確認した。

### 気づき

- 要件定義では機能だけでなく、非機能要件やデータ管理まで整理する必要がある。
- 開発では実装よりも設計段階の品質が後工程へ大きく影響することを再認識した。
- 進捗を表形式だけでなく視覚的に追える形にすると、管理しやすさが上がる。

## 2026-07-08

### 記録なし

- 学習ログとして残す具体的な記録はなし。
- 7 月前半の要件定義・FE 試験・Spring Boot 学習の流れを整理する期間として扱う。

## 2026-07-07

### 1. システム開発ドキュメント

- システム開発ドキュメントの構成を確認した。
- レビュー記録の扱いを整理した。
- 設計書の構成について確認した。

### 2. FE Subject B

- ミニマックス法の問題解説に取り組んだ。
- 三目並べを題材に、最善手を選ぶ考え方を整理した。
- 答えが「ア」になる理由を、探索木と評価値の流れから確認した。
- アルファベータ枝刈りの概念も確認し、不要な探索を省くことで効率化できることを理解した。

### 3. キャリア戦略

- SIer 営業職を中間ステップとする二段階戦略を整理した。
- 営業経験を活かしながら IT 業界に入り、そこから技術職・PMO・上流工程へ広げるルートを確認した。
- 電験三種を来年取得する計画も確認した。

### 気づき

- レビュー記録は要件定義書とは別管理が一般的。
- ドキュメントを分割管理すると保守性が向上する。
- ミニマックス法は「全部の手を暗記する」のではなく、相手も最善手を選ぶ前提で評価値を逆算する考え方が重要。

## 2026-07-06

### 要件定義書の構成整理

- 画面一覧を整理した。
- 制約事項を整理した。
- スケジュールを作成した。
- 要件定義書全体の構成を確認した。

### 気づき

- 画面一覧を整理すると、画面遷移や必要な機能が把握しやすくなる。
- 制約事項を明文化しておくことで、開発中の認識違いを減らせる。

## 2026-07-05

### 非機能要件・データ要件

- 非機能要件を整理した。
- データ要件を確認した。
- パスワード要件、データ保持期間、バックアップ・リストア方針、暗号化、個人情報管理について整理した。

### 気づき

- 非機能要件は、性能・可用性・セキュリティなど品質面を定義するもの。
- データ管理方針を明文化することで、運用時のトラブルを防ぎやすくなる。

## 2026-07-04

### 1. FE 試験

- CPU 使用率計算を学習した。
- プロセススケジューリングの基本を確認した。
- I/O 待ちが発生している間、CPU がどのように別プロセスを処理できるかを整理した。

### 2. 要件定義・システム開発工程

- 要件定義書について学習した。
- システム開発工程における要件定義の位置づけを確認した。
- 利用者と機能要件の整理を行った。

### 気づき

- CPU 使用率は、単に CPU が速いかどうかではなく、待ち時間・I/O・プロセス切り替えを含めて考える必要がある。
- 要件定義は「何を作るか」を決める工程。
- 実装方法ではなく、利用者が必要とする機能を明確にすることが重要。

## 2026-07-03

### 1. Java / Spring Boot

- Java と Spring Boot の違いを整理した。
- Java はプログラミング言語であり、Spring Boot は Java で Web アプリを効率的に開発するためのフレームワークだと確認した。
- フレームワークの役割を整理した。

### 2. キャリア

- PMO の役割について情報収集した。
- プロジェクト管理、進捗管理、課題管理、関係者調整など、PMO が担う領域を整理した。
- 電気主任技術者資格についても情報収集した。

### 気づき

- 言語とフレームワークは役割が異なる。
- PMO は技術だけでなく、調整力・ドキュメント作成力・進捗を見える化する力が重要。
- 営業経験で培ったコミュニケーション力や調整力は、PMO 領域でも活かせる可能性がある。

## 2026-07-02

### 1. 基本情報技術者試験

- ニュートン法を学習した。
- CPU スケジューリングを確認した。
- I/O 待ちと CPU 利用率の関係を整理した。
- プロセスの実行順序を確認した。

### 2. Spring Boot

- JWT 認証付き CRUD REST API を実装した。
- Spring Security、JPA、H2 を使った構成を確認した。
- Phase 1〜4 を完了し、バリデーション・例外処理まで実装した。
- 実装内容を GitHub に push した。

### 3. 仮想通貨

- GRASS エアドロップの仕組みを調査した。
- トークノミクスについて確認し、配布設計や参加条件の考え方を整理した。

### 気づき

- CPU 使用率は「CPU が実際に動作している時間」で決まる。
- I/O 待ち中は他のプロセスへ CPU が割り当てられるため、並列実行の考え方が重要。
- JWT 認証付き CRUD API まで作ると、単なる Hello World から一段進んだ実装経験になる。

## 2026-07-01

### 1. 基本情報技術者試験

- 二分探索法を学習した。
- 無向グラフを確認した。
- アルゴリズム問題に取り組んだ。
- 擬似コードの読解を行った。

### 2. FE Subject B 演習

- ビンソートを学習した。
- ユークリッドの互除法を確認した。
- `function1` / `function2` を使った関数最適化問題に取り組んだ。
- 配列反転（軸対称）の処理をトレースした。

### 気づき

- 二分探索法は「探索範囲を半分ずつ減らすこと」が本質。
- グラフ問題では、頂点と辺の関係を整理すると理解しやすい。
- アルゴリズムは暗記ではなく、**なぜ効率的なのか** を理解することが重要。

## 2026-06-30

### 1. 基本情報技術者試験 科目 B

- 五数要約（最小・第 1 四分位・中央値・第 3 四分位・最大）を求める疑似コード問題に取り組んだ。
- `summarize` / `findRank` 関数の構造を確認した。
- 戻り値、ローカル変数、関数ごとのスコープの考え方を整理した。
- 疑似コードでは、処理の流れだけでなく「どの関数が何を返しているか」「変数がどこまで有効か」を追うことが重要だと確認した。

### 2. Excel

- 進捗率に応じてセルを横方向に色分けする方法を学習した。
- 条件付き書式と `COLUMN()` 関数を組み合わせて、進捗に応じた見た目の変化を作る方法を確認した。
- 単なる表入力ではなく、進捗状況を視覚的に把握しやすくする工夫として整理した。

### 3. Spring Boot

- JWT 認証付き CRUD REST API を完成させた。
- Phase 1 として、バリデーション・例外処理を実装した。
- `@Valid` による入力チェック、`GlobalExceptionHandler` による共通例外処理、カスタム例外クラスの実装を行った。
- 実装内容を `git commit` & `push` まで完了した。

### 気づき

- 科目 B では、関数の分割・戻り値・ローカル変数のスコープを正確に追う力が重要。
- Excel の条件付き書式は、学習管理や進捗管理にも応用できる実務的な機能だと感じた。
- Spring Boot は CRUD 実装に加えて、バリデーションと例外処理まで入れることで、より実務に近い API になる。

## 2026-06-24

### 1. Spring Boot CRUD API 実装

- IntelliJ で CRUD API の実装を実際にコーディングした。
- `application.properties` に DB 接続設定を追加した。
- `entity/User.java`、`repository/UserRepository.java`、`service/UserService.java`、`controller/UserController.java` を順番に作成した。
- ファイル名・パッケージ配置のミスを都度修正した。`Refactor` → `Rename` の流れも確認した。
- メソッドがクラス外に書かれていたミスを発見し、クラス構造を意識して修正した。

### 2. Laravel と Spring Boot の対応整理

| Laravel | Spring Boot |
|---------|-------------|
| Model | Entity |
| Eloquent / DB 操作 | Repository |
| Service | Service |
| Controller | Controller |
| `.env` / config | `application.properties` |

Laravel で見てきた層構造と Spring Boot の構成を対応させることで、Java 側のファイル分割の意味が掴みやすくなった。

### 3. 基本情報技術者試験 科目 B

- 「4 の倍数カウント処理」の擬似コードトレース問題に取り組んだ。
- ダミー変数（上限用）の役割を確認した。
- `tempN mod 4 = 0` の条件と、最大 3 回のインクリメントで必ず処理が終わる理由を整理した。

### 気づき

- Spring Boot はファイル配置・パッケージ名・クラス内外の位置が重要で、構造のズレがそのままエラーにつながる。
- Laravel との対応関係を持っておくと、初見の Java / Spring Boot でも「これは何の役割か」を推測しやすい。
- 科目 B は処理の意味だけでなく、変数の役割と終了条件を丁寧に追うことが重要。

## 2026-06-23

### 1. Spring Boot 復帰・基礎再確認

- 5 日間のブランク後に Spring Boot 学習へ復帰した。
- `HelloController` の各行を詳細に確認した。
- REST API の URL 設計原則として、**名詞 + HTTP メソッド** の考え方を整理した。
- `HelloResponse` クラスの役割を DTO として理解し、Java オブジェクトが JSON に変換される仕組みを確認した。
- コンストラクタと `this` キーワードの意味を整理した。

### 2. 番兵法・HTTP メソッドの整理

- 番兵法（sentinel method）の概念を学習した。
- `@PostMapping` と `@GetMapping` の違いを確認した。
- `@RequestBody` が JSON → オブジェクト変換を担当する点を整理した。
- HTTP メソッドには慣習としての意味がある一方、技術的に完全強制されるわけではないことも確認した。

### 3. 基本情報技術者試験 科目 B 対策

- SR フリップフロップの不定状態（`S=1` かつ `R=1`）を理解した。
- スーパースカラ、VLIW、パイプライン、スーパーパイプラインの違いを比較した。
- 科目 B 集中 1 ヶ月プランを策定した。

| 期間 | 方針 |
|------|------|
| 第1〜2週 | 時間制限なしのトレース練習（1 日 3〜5 問） |
| 第3週 | 時間制限あり練習 |
| 第4週 | 模擬試験本番形式 |

### 気づき

- ブランク後は、動くコードをもう一度読み直すことで感覚を戻しやすい。
- REST API は URL、HTTP メソッド、リクエストボディ、レスポンス DTO の関係で見ると整理しやすい。
- 科目 B は知識暗記より、時間内に落ち着いてトレースできる状態を作ることが重要。

## 2026-06-22

### 基本情報技術者試験 前日・最終追い込み

科目 A・B の両方について、大規模な試験対策を行った。

| 分野 | 学習内容 |
|------|----------|
| アルゴリズム | 単方向連結リスト、二分木探索（中順）、クイックソート、挿入ソート、二分探索、ビンソート、ユークリッドアルゴリズム |
| ハードウェア | 2 の補数、浮動小数点誤差、シフト演算、キャッシュメモリ（ライトスルー / ライトバック） |
| ネットワーク | OSI 参照モデル、サブネット計算、各層のデバイス |
| セキュリティ | CIA トライアド、各種攻撃手法と対策 |
| 信頼性設計 | フェイルセーフ、フォールトトレラント、フールプルーフ |

### 気づき

- 科目 B は、問題文を読みながら変数・条件・ループを正確に追う力が重要。
- セキュリティやネットワークは知識問題として整理しつつ、科目 B では時間配分を崩さないことが大事。

## 2026-06-19

### 基本情報技術者試験 本番

- 基本情報技術者試験を受験した。
- 科目 A は **635 点で合格**。
- 科目 B は **515 点で不合格**。
- 科目 B の失敗原因として、時間不足と、セキュリティ問題に時間を取られすぎたことを整理した。
- 約 1 ヶ月後の再受験を決定した。

### 気づき

- 科目 A に合格できたことで、基礎知識の積み上げは形になってきている。
- 科目 B は時間管理とアルゴリズムトレースが課題。
- 次回は「分かる」だけでなく、制限時間内に解き切る練習が必要。

## 2026-06-17

### 1. Java 基礎・IntelliJ 環境構築

- JDK / JRE / JVM の役割を整理した。
- Java の OOP（オブジェクト指向）概念を確認した。
- PHP との違いを比較しながら、Java の書き方や実行の流れを理解した。
- IntelliJ IDEA で Java 学習用プロジェクトを作成し、`Hello World` REST API を実行した。
- Spring Initializr を使って Spring Boot プロジェクトを作成し、動作確認まで完了した。
- 最初の Java / Spring Boot 学習内容を Git で初コミットした。

### 2. Spring Boot REST API ハンズオン

4 つの REST API パターンを実装した。

| パターン | 内容 |
|----------|------|
| 文字列返却 | `GET` で文字列レスポンスを返す |
| パスパラメータ | `@PathVariable` で URL の一部を受け取る |
| JSON レスポンス | `HelloResponse` クラスを使って JSON を返す |
| POST リクエスト | `@RequestBody` でリクエストボディを受け取る |

- `@RestController`、`@GetMapping`、`@PathVariable`、`@RequestBody` の役割を Laravel と比較しながら整理した。
- IntelliJ HTTP Client を使って POST リクエストをテストした。
- ハンズオン中に発生したエラーを 3 件解決し、Spring Boot のリクエスト処理の流れを理解した。

### 3. 30 日間の Java / Spring Boot 学習スケジュール策定

| Phase | 期間 | 内容 |
|-------|------|------|
| Phase 1 | Day 1-12 | Java 基礎 + Web 基礎並行 |
| Phase 2 | Day 13-18 | Spring Boot 入門 + CRUD API |
| Phase 3 | Day 19-24 | Spring Data JPA + SQL |
| Phase 4 | Day 25-30 | Spring Security + JWT 認証 |

最終目標は **JWT 認証付き CRUD REST API + Swagger ドキュメント**。

### 4. Cloudflare Tunnel 検討

- `jikoseityo.com` を Cloudflare 管理下に移し、Laravel アプリをスマホから確認できる構成を復習した。
- `docker-compose.yml` に `cloudflared` サービスを追加する構成を確認した。
- Cloudflare Tunnel を使うことで、ローカル開発環境を安全に外部公開する流れを整理した。

### 気づき

- Java は PHP と比べて型やクラス構造を強く意識するため、最初は重く感じるが、API 開発や大規模開発では設計の見通しが立てやすい。
- Spring Boot はアノテーションの意味を押さえると、ルーティング・リクエスト受け取り・レスポンス返却の流れが見えやすくなる。
- 学習計画を Phase に分けたことで、最終目標の JWT 認証付き CRUD REST API までの道筋が明確になった。

## 2026-06-16

### 1. ネットワーク基礎（インターネット接続・通信の仕組み）

| トピック | 内容 |
|----------|------|
| 接続状態の違い | 「インターネット未接続」「接続速度が遅い」「Web に繋がらない」を技術的に区別 |
| URL 構造 | スキーム・ホスト・パス・クエリストリング・フラグメントの各要素 |
| DNS の名前解決 | ブラウザキャッシュ → フルサービスリゾルバ → ルート DNS → TLD → 権威 DNS の流れ |
| IP アドレスとポート番号 | ホスト（家）とポート（部屋）のアナロジー、ウェルノウンポート（80, 443, 22, 3306, 5432） |
| Laravel との接続 | `php artisan serve` のデフォルトポート `8000` はウェルノウンポートではないことを確認 |

### 2. AI エージェント開発の環境構築検討

- **目的**: メール・スケジュール・タスク管理を統合した業務自動化エージェントの構築。
- **構成決定**: Gmail + Google Calendar + Claude API の最小構成からスタート。
- **理由**: 社内は Outlook / Teams の管理者権限制約があるため、まずはプライベート環境で開発する方針。
- **流用可能性**: SES Matching アプリのメール処理コードを流用できると確認。

### 気づき

- URL のパス部分が Laravel の `routes/web.php` に直結していることを再確認し、RESTful な URL 設計の重要性を体感した。
- DNS の解決チェーンは **キャッシュを先に見る** 設計が性能に直結している点が面白いと感じた。

## 2026-06-15

### Claude Code（VS Code 拡張）のセットアップ

- VS Code の Claude Code 拡張のログイン画面を確認・設定した。
- 認証オプションの違いを整理した。

| 選択肢 | 対象 |
|--------|------|
| Claude.ai Subscription | Pro / Team / Enterprise プラン保持者 |
| Anthropic Console | API キー・従量課金ユーザー |
| Bedrock / Foundry / Vertex | AWS / Azure / GCP 経由利用者 |

Claude.ai Subscription を選択してブラウザ認証する手順を確認した。

## 2026-06-13

### 学習ログの更新（GitHub への反映確認）

- `furuyaaaa/furuyaaaa/LEARNING_LOG.md` が 6 月 6 日以降未更新であることを確認した。
- 6 月 7〜12 日分の学習内容をログへ追記・整形した。
- GitHub への push コマンドを確認した。

```bash
git add LEARNING_LOG.md
git commit -m "学習ログ 2026-06-09〜06-12 追記"
git push origin main
```

## 2026-06-12

### 基本情報技術者試験 科目 A 学習（ハードウェア・メモリ）

| トピック | 学習内容 |
|----------|----------|
| メモリインタリーブ | 複数バンクに分割しアクセスを並列化することでメモリ帯域を向上させる仕組み |
| キャッシュ書き込み方式 | ライトスルー（即時書き込み）とライトバック（遅延書き込み）の違いと使い分け |
| CPU・キャッシュ・RAM の階層 | L1 / L2 / L3 キャッシュの物理配置と速度・容量のトレードオフ |
| DRAM / SRAM / フラッシュ | リフレッシュ要否・速度・コスト・用途の比較 |
| 磁気ディスク性能計算 | RPM から平均回転待ち時間、シーク時間を合計して平均アクセス時間を算出 |
| RAID 構成 | RAID0（ストライピング）、RAID1（ミラーリング）、RAID5（パリティ分散）の特性 |
| DPI 計算 | ドット数 ÷ インチ数、単位換算の計算手順 |

### 気づき

- RAID は AWS SAA（EBS の RAID 設定）にも関連するため、優先的に押さえる価値がある。
- CPU のキャッシュメモリはチップ内部に物理的に搭載されていることを確認した。

## 2026-06-11

### DevOps 資格・CI/CD の概念学習

DevOps 関連資格の全体像を整理した。

| カテゴリ | 資格例 |
|----------|--------|
| クラウドベンダー | AWS CDA（DVA-C02）、Google Cloud DevOps Engineer |
| ベンダーニュートラル | CKA（Kubernetes）、Terraform Associate、Docker |
| IT プロセス | ITIL 4、DevOps Foundation |

### AWS CDA（DVA-C02）の試験ドメイン

- Lambda, API Gateway, DynamoDB
- CI/CD ツール（CodePipeline, CodeBuild, CodeDeploy）
- モニタリング（CloudWatch, X-Ray）

### CI/CD の概念

| 用語 | 意味 |
|------|------|
| CI（継続的インテグレーション） | コードを main ブランチに頻繁にマージし、自動テストを実行する |
| CD（継続的デリバリー） | テスト済みコードを自動でステージング / 本番環境へデプロイする |
| 代表ツール | GitHub Actions, AWS CodePipeline, CircleCI, Jenkins |

### 気づき

- Laravel プロジェクトはすでに GitHub で管理しているため、GitHub Actions が最初の CI/CD 導入として最適。
- 推奨資格取得順は **AWS SAA → CDA → DevOps Engineer Professional**。

## 2026-06-09

### OSS-DB Silver 試験対策（PostgreSQL 管理コマンド）

#### 問121: `pg_dump` オプション

| オプション | 内容 |
|------------|------|
| `-s` | スキーマ定義のみ出力（データなし）← 正解 |
| `-a` | データのみ出力（スキーマなし） |
| `-c` | オブジェクトの DROP コマンドを追加出力 |
| `-O` | オーナー設定（ALTER OWNER）を出力しない |

- `-s`（schema-only）と `-a`（data-only）は対の関係として覚える。
- スキーマ定義（CREATE TABLE・インデックス・ビュー等）とデータレコードは明確に別物。

#### 問122: `VACUUM FULL` の動作

| 項目 | VACUUM（通常） | VACUUM FULL |
|------|----------------|-------------|
| ロック | なし | ACCESS EXCLUSIVE（全操作ブロック） |
| ファイル縮小 | なし | あり（新テーブルを作成して置き換え） |
| 本番環境 | 安全 | 長時間ロック発生リスクあり |

本番環境では `pg_repack`（ロックなし代替）が推奨される。

### 気づき

- `-s` オプションは「スキーマ（= 設計図）だけ持ち出したい」場面（本番 → 開発環境複製）で使う。
- `VACUUM FULL` は **全操作ブロック** が試験の頻出ポイント。

## 2026-06-07

### 記録なし・休息日

- 学習記録はなし。
- 休息日として整理。

## 2026-06-06

### 資格・技術学習

- SQL 学習サイトについて調査した。
- 基本情報技術者試験関連として、アルゴリズム・データ構造・論理回路まわりを学習した。

### 学習したトピック

- スタック（Stack）
- キュー（Queue）
- マージソート
- 文字列探索アルゴリズム
- 補数
- ド・モルガンの法則
- 論理回路問題

### 気づき

- コードを一文字ずつ追うより、**アルゴリズム全体の流れを理解することが重要**だと確認した。
- 自分には **変数の動きを追う癖がある**ことに気づいた。これは処理の流れを理解するうえで強みになる一方、細部に入りすぎると全体像を見失いやすいので、まず全体の流れを掴んでから変数を追う意識が大事だと整理した。

## 2026-06-05

### 1. SQL 練習（pgexercises.com ＋ リテールデータセット）

`daily_retail_data` テーブルを使った実践クエリを中心に、以下の構文・関数を学習した。

| トピック | 内容 |
|----------|------|
| `WHERE IN` | 複数条件を一括指定する構文 |
| `DISTINCT` | 複数カラムにまたがる重複除去 |
| `UNION` / `UNION ALL` | 重複排除の有無の違い |
| `LIMIT` + `ORDER BY` | 件数制限と並び順の組み合わせ |
| 日付関数 | `EXTRACT`、`generate_series`、`::timestamp` 型キャスト |
| `COUNT(*) AS` | 集計結果への別名付け |
| `LIKE` | ワイルドカードを使ったあいまい検索 |
| `JOIN` + テーブル別名 | 複数テーブルの結合とエイリアス |

実際にクエリを書いて提出し、添削を受けるサイクルで練習。`DESC` と `ASC` の書き間違い、`SELECT *` の省略ミス、`COUNT(*)` への `AS` 付け忘れなど、実際のミスを通じて定着させた。

### 2. Laravel `Validator.php` ソースコード読み

以下のメソッドを読み進めた。

- `validateAttribute`
- `dependsOnOtherFields`
- `isValidatable`
- `isImplicit`
- `passesOptionalCheck`
- `isNotNullIfMarkedAsNullable`
- `hasNotFailedPreviousRuleIfPresenceRule`
- `validateUsingCustomRule`

**掴んだ感覚**: 早期リターン（`return true` / `return false`）を積み重ねてバリデーション可否を判定するパターンが繰り返し登場することに気づき、構造の読み解き方が身についてきた。

### 3. 重要な方針転換（学習戦略の見直し）

この日の最大の収穫は、**「Laravel ソースを 1 行ずつ読むことは最善ではない」** という結論に至ったこと。

AI が実装を担う時代において、自分の本当の価値は **要件定義・AI への正確な指示・結果の検証** にある、という認識を固めた。一方で SQL は **AI が生成したクエリの検証** に不可欠なため、引き続き重点的に学ぶべき領域として位置づけを明確化した。

SQL 実践と方針転換という、技術面と戦略面の両方で充実した 1 日だった。

## 2026-06-03

### 1. Laravel ソースコードリーディング（Validator.php）

#### `passes()` メソッドの全体像

- 冒頭で `MessageBag` をリセットし、`distinctValues` と `failedRules` を初期化する
- `$rules` を「フィールド × ルール」の二重ループで順番に検証する
- `shouldBeExcluded()` は除外対象のフィールドをスキップする役割
- `shouldStopValidating()` は同じフィールド内で、これ以上ルール検証を続けるかを判定する役割
- `stopOnFirstFailure` は最初の失敗後に全フィールドの検証を止める
- `bail` は同じフィールド内だけ後続ルールを止める
- `after` フックは通常のルール検証が終わったあとに実行される
- 最後に `return $this->messages->isEmpty()` でエラーがなければ `true`、あれば `false` を返す

#### 関連メソッド

- `shouldBeExcluded()`: ドット記法の子要素も含めて除外対象かを判定する
- `removeAttribute()`: `data` と `rules` の両方から対象属性をセットで削除する
- `validate()`: バリデーションに失敗したら例外を投げる窓口メソッド
- `validateWithBag()`: 複数フォームがある場合に、どのエラーバッグへ入れるか指定できる
- `safe()`: 検証済みデータを `ValidatedInput` オブジェクトとして返す

### 2. PHP 構文

- `if` + `continue` は、条件分岐とループ継続命令を組み合わせたもの
- `public` / `protected` / `private` がついた時点で、プロパティまたはメソッドの定義になる
- `= []` は初期値の設定であり、それ自体がプロパティかどうかを決めるわけではない
- `null` は「値がない」、空の配列 `[]` は「配列だが中身がない」状態
- `?array` は nullable 型で、配列または `null` を受け取れる
- `[$a, $b] = [[], []]` は分割代入で、複数の変数へまとめて値を入れられる

### 3. 基本情報技術者試験 過去問

#### 稼働率

- 直列: 稼働率をかけ算するため、弱い方に引っ張られる
- 並列: `1 - (1 - 稼働率)^n` で、冗長化により補い合う

#### テスト設計の網羅性

- 命令網羅（C0）: 全命令を 1 回実行する
- 判定条件網羅（C1）: 全分岐を 1 回実行する
- C1 は C0 の上位基準なので、C1 を満たせば C0 は自動的に満たされる

### 振り返り

- Laravel の `passes()` を起点に、検証処理の大きな流れと PHP 構文の読み方を整理できた
- 基本情報の稼働率とテスト網羅性も、公式や用語暗記ではなく「なぜそうなるか」で理解を進められた

## 2026-06-02

### 1. ネットワーク基礎

#### SSL/TLS

- 通信を暗号化する仕組み
- HTTPS は 443 番ポート
- DB はポートそのまま（PostgreSQL なら 5432）で接続し、接続時に SSL にアップグレードする

#### TCP/IP

- TCP（4層）: 確実に届ける
- IP（3層）: 宛先を決める
- SSL は TCP/IP の上に乗っかって暗号化を追加する

#### OSI 7層モデル

- 7層: アプリケーション（HTTP, SSH）
- 4層: トランスポート（TCP）
- 3層: ネットワーク（IP）

#### ポート番号

- 22: SSH
- 80: HTTP
- 443: HTTPS
- 5432: PostgreSQL

### 2. PHP / Laravel 基礎

#### callback

- 後で呼び出す関数を別の関数に渡す仕組み
- 処理の一部を外から差し込める

#### callable

- 呼び出せるもの全般の型
- クロージャ・無名関数・`[$object, 'method']` 形式など

#### メソッドチェーン

- `return $this` によって同じオブジェクトに対して操作を続けられる
- `DB::table()->where()->orderBy()->get()` のような書き方

### 3. Laravel の `after()` ソースコード追跡

#### `after()` メソッド

- バリデーション後に追加で実行したい処理を登録するメソッド
- `$callback` が配列なら 1 個ずつ再帰処理する
- 配列以外ならアロー関数でラップして `$this->after[]` に登録する

#### `validate()` から `passes()` までの流れ

```text
validate()          ← 窓口
    ↓
fails()/passes()    ← 核
    ↓
validateAttribute() ← 個別ルールチェック
    ↓
$this->after 実行   ← after() で登録したものがここで動く
    ↓
messages->isEmpty() ← 結果判定
```

#### `method_exists()`

- オブジェクトにメソッドが存在するか確認する関数

#### ファーストクラス Callable 構文

- `$rule->after(...)` でメソッド自体を関数として取り出せる

### 振り返り

- ネットワークの基礎から Laravel の内部実装まで、幅広く追いかけた 1 日だった

## 2026-06-01

### 基本情報技術者試験

- MTBF / MTTR の意味と稼働率との関係
- 予防保守は MTBF 向上につながる
- 遠隔保守は MTTR 短縮につながる
- MPEG-4 は携帯端末・低速回線向けの動画符号化方式

### Laravel / PHP（Validator 読解）

#### プロパティの理解

- `protected $fileRules = [...]` はクラスのプロパティ定義
- 配列の中身はただの文字列（名前のリスト）
- 縦書き・横書きは見た目だけで、動作は同じ

#### 設計の理解

- `$fileRules`・`$excludeRules` などでルールをカテゴリ分けして管理
- `in_array()` で「このルール名、リストに入ってる？」と照合して処理を分岐

#### `passes()` メソッドの流れ

- `MessageBag` 初期化
- 全ルールをループ
- 除外対象はスキップ
- `validateAttribute()` で各ルールを検証
- エラー 0 件なら `true` を返す

## 2026-05-31

### 学んだこと

- Stack と Queue の違い
- Stack は後入れ先出し（LIFO）
- Queue は先入れ先出し（FIFO）
- MergeSort: 分割してから結合するソート
- while / endwhile の対応を意識することが重要
- 一気に理解しようとせず、処理の流れを追う
- QuickSort: 基準値（pivot）で分割する
- 小さいグループと大きいグループへ分ける
- 再帰的に処理を行う
- 文字列探索: index を順番に動かして比較する
- Sidx / Aidx の意味を理解
- 一致・不一致で処理が変わる

### プログラム読解で学んだこと

- if / endif: 条件分岐の終了位置
- 条件成立時のみ処理を実行
- for / endfor: 繰り返し処理
- ループ終了位置を明確にする
- while / endwhile: 条件が成立している間繰り返す
- 開始位置と終了位置の対応が重要

### ビット演算・論理演算

#### 学習内容
- AND
- OR
- NAND
- 補数
- ド・モルガンの法則

#### 学んだこと

- 補数: 0 と 1 を反転させる考え方。2進数計算で重要。
- ド・モルガンの法則: NOT( A AND B ) / NOT( A OR B ) の変形ルール。
- 論理回路問題で頻出。

### SQL / PostgreSQL

#### 学習内容
- CASE WHEN
- JOIN
- サブクエリ
- GROUP BY
- ORDER BY
- 集計処理

#### 学んだこと

- CASE WHEN: 条件によって値を切り替える。

```sql
CASE
  WHEN 条件 THEN 値
  ELSE 値
END
```

- サブクエリ: 一時的な仮テーブルのように扱える。外側SELECTと内側SELECTを分けて考える。
- JOIN: 複数テーブルを結合する。INNER JOIN を利用してデータを結合する。

### Laravel / プログラミング

#### 学んだこと

- モジュール: 機能単位で分割された部品。ファイルごとに役割を分ける。
- SOLID: 設計原則。単一責任、拡張に強い設計、依存関係の整理などが重要。

### GitHub 活用方針

#### 今後やること
- GitHub repository 作成
- 学習ログを継続
- 小さくても動くアプリを作る

#### 方針
- AI（Codex）を活用
- コードを読みながら理解
- README を充実させる

### 気づき

- コードを全部暗記する必要はない
- 「流れ」を理解することが重要
- 変数の役割を意識すると読みやすい
- 実際に手を動かすと理解しやすい
- AIを使いながら作る時代になっている

### 次の目標
- GitHub へ学習ログ公開
- SES案件・人材管理アプリ作成
- Laravel / PostgreSQL 学習継続
- 基本情報技術者試験 合格

## 2026-05-30

### 学習まとめ（2026-05-30）

大きく分けると「アルゴリズム・データ構造」「SQL/PostgreSQL」「情報セキュリティ」「システム設計」「キャリア理解」の5本柱を学びました。  

① アルゴリズム・データ構造

かなり“コードを読む力”を鍛えていました。  
- マージソート（Merge Sort）: while と endwhile、for と endfor、配列を分割 → 併合する流れ、対応する end を確認。  
- 文字列探索アルゴリズム: Sidx / Aidx、一致・不一致時の動き、配列インデックス管理、どこを進めるかの判断。  
- スタック・キュー: FIFO / LIFO、荷物移動ゲーム的な考え方、queue と stack が混ざった問題に着手。  
- ミニマックス法・逆ポーランド記法系: priority / opcnt、演算子の優先順位、状態管理（変数は状態管理かカウンタかフラグかを意識）。  

変数を「役割」で見る癖（例: `i, idx`=位置管理、`cnt`=カウンタ、`flag`=状態、`head/tail`=先頭末尾管理）が重要だと改めて確認しました。  

② SQL / PostgreSQL

実践的な問題に取り組みました。  
- 学習トピック: JOIN、CASE WHEN、サブクエリ、GROUP BY、SUM、RANK() OVER()、PostgreSQL のエラー解析。  
- 問題例: 高額予約抽出での CASE 分岐（ゲスト料金／会員料金）、投資回収期間問題（初期費用・月次利益・維持費・回収月数の算出）。  

「サブクエリ = 一時的な表を作る」という視点が芽生え、仮テーブルを使って複雑処理を分割する発想が身につきました。  

③ 情報セキュリティ・ネットワーク

基本情報で頻出の分野を整理しました。  
- 学習トピック: WAF、RSA、CA（認証局）、DMZ、BYOD、SOA、SQLインジェクション、改ざん検知、C&C、IPv6、ディジタルデバイド。  
- 特に WAF（Webアプリ攻撃対策）と RSA/CA（公開鍵暗号と証明書）の理解が深まりました。  

④ システム設計・開発知識

モジュール設計、SOLID、クイックソート、オートマトン、3層クライアントサーバなどを確認し、ファイル単位での分割とモジュール責務の整理を実践的に学びました。  

⑤ 学習スタイルとして良かった点

「答えを暗記する」のではなく、なぜ動くのか／何を管理しているのか／なぜここで終了するのか、を問い続ける学び方が出来ていた点がとても良かったです。  

総評: 昨日は多くの「点」が増え、点→線→面へつなげる直前にいる感触。特にアルゴリズムは while/if/idx/配列/状態管理が繋がる瞬間が来るので、継続して読み進めていくことが重要です。

## 2026-05-29

### 本日学んだこと

今日は HasOneOrMany.php、HasMany.php、BelongsTo.php を中心に読み進め、最後に現場のテストコードも読みました。  
まず __construct() から読み始め、foreignKey や localKey などの必要な情報をセットしてから parent::__construct() で親クラスの Relation に共通処理を委譲する仕組みを理解しました。parent:: が「継承元のクラスのメソッドを呼ぶ」という意味であることも理解できました。  
addConstraints() では $user->posts と書いたときに WHERE posts.user_id = 1 という WHERE 句が自動的に組み立てられる仕組みを理解しました。static::$constraints というスイッチによって通常時は WHERE 句が付き、Eager Loading のときは一時的に OFF になることもわかりました。  
buildDictionary() と matchOneOrMany() では、Eager Loading で取得した全データをインデックスのような辞書に変換してから各モデルに高速に紐付ける仕組みを理解しました。辞書がデータベースのインデックスと同じ発想であることも気づけました。  
BelongsTo.php では associate() と dissociate() を読み、外部キーのセットと解除の仕組みを理解しました。HasMany が「1対多の1側」で BelongsTo が「1対多の多側」という関係も整理できました。また自己参照リレーションという同じテーブルが自分自身を参照する仕組みも理解しました。  
最後に現場のテストコードを読み、Pestというシンプルなテストフレームワークの書き方を理解しました。test('説明', function() { 処理 }) という構文の繰り返しでCRUDテストが書かれていることも確認しました

## 2026-05-28

### 5/28学んだこと

5/28は HasOneOrMany.php、HasMany.php、BelongsTo.php の 3 つのファイルを読みました。  
まず __construct() から読み始め、foreignKey や localKey などの必要な情報をセットしてから parent::__construct() で親クラスの Relation に共通処理を委譲する仕組みを理解しました。引数がどこから来るのかを追いかけながら読むことで、クラス間のつながりが見えてきました。  
次に addConstraints() を読み、$user->posts と書いたときに WHERE posts.user_id = 1 という WHERE 句が自動的に組み立てられる仕組みを理解しました。static::$constraints というスイッチによって、通常時は WHERE 句が付き、Eager Loading のときは一時的に OFF になる仕組みも理解しました。  
buildDictionary() と matchOneOrMany() では、Eager Loading で取得した全投稿を user_id をキーにした辞書に変換してから、各ユーザーに対応する投稿を高速に紐付ける仕組みを読みました。  
最後に BelongsTo.php を読んで、HasMany が $user->posts（User から Post を見る）であるのに対して、BelongsTo は $post->user（Post から User を見る）という逆方向のリレーションであることを理解しました。同じ posts.user_id を使いながらも向きが逆になっているのが大きな発見でした。  
全体を通じて、$user->posts というたった一行の裏側に __construct() から getResults() まで多くの処理が動いていることが見えた一日でした。
## 2026-05-27

### `save()` の全体像

- **`mergeAttributesFromCachedCasts()`** … キャスト済みの値を **`$attributes`** に反映したうえで、**`newModelQuery()`** で **QueryBuilder** を生成する。  
- **`fireModelEvent('saving')`** … 保存前フック（**キャンセル可能**かどうかは後述の `halt`）。  
- **`exists` フラグ**で **INSERT (`performInsert`) / UPDATE (`performUpdate`)** を分岐。  
- **`finishSave()`** … **`saved` イベント**、**関連モデルのタイムスタンプ更新**、**`syncOriginal()`** による **`$original` のリセット** など、保存後の後処理。

### `isDirty()` と無駄 UPDATE 回避

**`$original` と `$attributes` を比較**して変更の有無を判定し、**変更がなければ無駄な UPDATE を出さない**設計。

### `fireModelEvent()` と `halt`

**`halt`** により、**保存前イベント**（例: `saving` / `creating` / `updating`）は **キャンセル可能**。**保存後**（`saved` / `created` / `updated`）は **キャンセル不可**の設計。

### Eloquent リレーション — `hasMany()`

- **外部キー・ローカルキーを自動決定**し **`HasMany` インスタンス**を生成。  
- **`addConstraints()`** で **`WHERE user_id = ?`** のような条件が **自動付与**されること。  
- これが **`N+1` の根本原因**になり得ることともつながって理解。

### 周辺の文法・概念

**継承** `Relation` → `HasOneOrMany` → `HasMany`、**`parent::__construct()`** による親初期化、**`tap()`**、**`iterable` 型**、**PHPDoc アノテーション** などを整理。

### 現場のつながり（H01001m）

**Blade → `Page.php` → `SlipMemoService` → `slip_memos`** までの一本線が掴めた。さらに画面の **F キー** → **`$wire.call()`** → **Livewire** → **`$model->save()`** → **QueryBuilder** → **PostgresGrammar** → **Aurora PostgreSQL** まで、**保存の全連鎖が頭の中で繋がった**一日。

## 2026-05-26

### 1. `wire:model` とは

`wire:model` は **HTML の入力と PHP の `public` プロパティを Livewire が自動同期**する仕組み。ユーザーが入力すると **サーバー側プロパティにリアルタイムで反映**される。

同期タイミングは **修飾子**で変える。**`wire:model.blur`** はフォーカスが外れたとき、**`wire:model.live`** は入力のたび。**`.live` をむやみに使うと DB アクセスが連打**されることがあるので、プロジェクトで **`.blur` / `.live` のどちらが使われているか**を見ると「なぜ `.blur` か」の設計意図につながる。

プロパティが変わると **`updated` + プロパティ名** のメソッドが自動で呼ばれる。

### 2. `page.js` のイベント委譲

数百セルを **document 直下のリスナー 2 本** で賄うパターン（例: **113〜146 行付近のイメージ**）。

```javascript
document.addEventListener("blur", ...)    // 全セル共通
document.addEventListener("keydown", ...) // * キー用など
```

各セルに個別バインドせず、`document` で受け、`class="b00201m-cell-input"` と **`data-*`** で対象セルを特定する。**イベント委譲**で、セルごとにリスナーを増やすと重くなる問題を避けるパフォーマンス手法。

### 3. Eloquent と SQL の対応

例: `where('unit_id', $keys['unit_id'])` は SQL の **`WHERE unit_id = ?`** に対応。**左辺はカラム名（固定的）**、**右辺は呼び出し元からの動的值**。

`unit_id`（内部 ID）と `unit_code`（表示用）は **それぞれセッションから取得**しており、この例では相互変換はしていない。**`getGlobalUnitId()`** は `Page.php` の **`private`** で、Blade からは叩かない想定。

### 4. `Page.php` と Blade の接続

`Page.php` の **`render()`** が `view('livewire.b00201m.page')` を返すことで **Livewire と Blade が接続**される。

ユーザー操作は **`wire:model`（ヘッダー）** と **`page.js`（セル・特殊キー）** の 2 経路で **`Page.php` に収束**。`Page.php` は Blade へ表示指示を出しつつ、**複数の Service** 経由で DB・業務ルールを処理し **PostgreSQL** と通信する、という流れ。

```text
ユーザー操作
    ├── wire:model（ヘッダー）
    └── page.js（セル・特殊キー）
            ↓
        Page.php
            ├── Blade（表示）
            └── Service × 複数（DB・業務ルール）
                    ↓
                PostgreSQL
```

### 5. SQL — `GROUP BY`

**同じ値の行をまとめ、グループ単位で集計**する。**部門ごと・月ごと**などの合計・平均に使う。

集計関数の例: `COUNT`, `SUM`, `AVG`, `MAX`, `MIN`。

```sql
SELECT department, COUNT(*) AS 社員数
FROM employees
GROUP BY department;
```

### 6. SQL — `HAVING`

**グループ化したあと**の結果をさらに絞る。**`WHERE` はグループ化前の行、`HAVING` はグループ化後の集計結果**への条件。 **`HAVING` には集計関数を書ける**が、**`WHERE` には原則集計関数を書けない**。

```sql
SELECT department, COUNT(*) AS 社員数
FROM employees
GROUP BY department
HAVING COUNT(*) >= 10;
```

### 7. SQL — `WHERE` と `HAVING` の使い分け

**`WHERE` → グループ化前、`HAVING` → グループ化後**。先に **`WHERE` で減らす**とグループ集計が軽くなり効率的。**書ける条件はできるだけ `WHERE` 側**がベターなことが多い。

```sql
SELECT department, AVG(salary) AS 平均給与
FROM employees
WHERE salary > 200000
GROUP BY department
HAVING AVG(salary) > 350000;
```

処理の順序イメージ: **① WHERE**（行絞り）→ **② GROUP BY** → **③ HAVING**（グループ絞り）→ **④ SELECT**（表示）。

### 8. SQL — `GROUP BY` とよくある間違い

`GROUP BY` 使用時、`SELECT` に出せるのは原則 **`GROUP BY` に並んだ列**か **集約式だけ**。

```sql
-- NG：name は GROUP BY にも集約にも含まれていない
SELECT department, name, COUNT(*)
FROM employees
GROUP BY department;

-- OK
SELECT department, COUNT(*)
FROM employees
GROUP BY department;
```

### 学び方メモ

- いきなり全部は理解しない。**「どこに何があるか」「ざっくりした流れ」** の **地図**を持つと未知のコードでも進める。  
- 成長イメージ: **読めない → 読める → 真似て書ける → 自分で書ける**。AI に書かせて終わりより、自分で **「なぜこう書いてあるか」を追う方が実力になりやすい**。

## 2026-05-25

### `Model.php` — `performInsert()` / `performUpdate()`

- **`performInsert()`**  
  - **UUID / ULID** を使うモデルでは **`setUniqueIds()`** で **INSERT 前にアプリ側で PK を生成**。  
  - **AUTO INCREMENT** の場合は **`insertAndSetId()`** で **INSERT 後に PostgreSQL の `RETURNING`** で DB 採番の PK を受け取る。  
    → **2 パターン**があると整理。  
  - **`fireModelEvent()`** で **Observer が処理をキャンセルできる**こと。  
  - **`usesTimestamps()`** により **`created_at` / `updated_at`** が自動セットされること。

- **`performUpdate()`**  
  **`performInsert()` と構造はほぼ同型**だが、INSERT が全カラム送りなのに対し、**`getDirtyForUpdate()` で変更のあったカラムだけ** を UPDATE に送る **重要な違い**。

- **設計・慣例**  
  **Trait／オーバーライド／Convention over Configuration**。クラス名からテーブル名を自動解決する考え方、**`$incrementing`・`$timestamps` を「例外だけ上書き」**するパターン。

- **周辺語彙の整理**  
  UUID・ULID・AUTO INCREMENT・`RETURNING`・PK・キャメルケース・スネークケース などを整理しながら読んだ。

- **気づき・一本の流れ**  
  画面の **F キー** → **`$wire.call()`** → **Livewire コンポーネント** → **`$model->save()`** → **`performInsert()` / `performUpdate()`** → **QueryBuilder** → **PostgresGrammar** → **`RETURNING id`** → **Aurora PostgreSQL** までが **一続きで繋がった**感覚を得た一日。

## 2026-05-22

### Model.php の読解

- **`newQuery()` の流れと QueryBuilder の完成**  
  `newQuery()` が内側で `newQueryWithoutScopes()` を呼び、さらに **`newModelQuery()` → `newBaseQueryBuilder()` → `getConnection()->query()`** と進み、最終的に **`PostgresGrammar` を内包した `QueryBuilder`** ができるまでを追った。**`User::where()` と書いた瞬間に裏で何が起きるか**が腹落ちした。

- **グローバルスコープと `save()`**  
  **`registerGlobalScopes()`** でグローバルスコープが装着される仕組み。`save()` 内の **`exists` フラグ**で **INSERT / UPDATE** が分岐すること。**`isDirty()`** で変更がなければ無駄な SQL を打たない工夫も整理。

### SQL の学習

- **サブクエリの置き場所**  
  **SELECT・FROM・WHERE** の 3 箇所で使える。**FROM 内**のサブクエリは **一時テーブル（派生テーブル）** として扱われる。**WHERE 内**で外側の行を参照するサブクエリは **相関サブクエリ**で、**行ごとに繰り返し評価**されやすい。

- **ウィンドウ関数**  
  **`RANK()` / `ROW_NUMBER()` / `NTILE()`** の違いを整理。`OVER()` が空なら全行対象、`OVER(ORDER BY …)` で順位付け。**`NTILE(n)`** は結果を **n 等分する**ユニークな関数。

- **その他（OSS-DB Silver 意識）**  
  **`CASE WHEN`** は SELECT だけでなく **集約関数の内側**にも書ける。**`trim` / `to_char`** は **OSS-DB Silver の出題範囲**。**`ROW_NUMBER`・`WITH` 句・移動平均** は Silver では **優先度が低め**とも確認。

## 2026-05-21

- **`users` と `tasks` の違い（Laravel CRUD）**  
  `users` は **ログインする人（アカウント）**、`tasks` は **その人のやることリスト**。`tasks.user_id` が `users.id` に外部キーで繋がり、**1 ユーザーに複数タスク**。

- **テーブル・行・カラムが増えるタイミング**  
  **ユーザー数・タスク数の増加** → 主に **行** が増える。**機能追加・改修** → **カラム**や **新テーブル** が増える。公開やアクセス増だけでテーブルが自動では増えない。

- **`migrations` テーブルと `database/migrations/` の PHP**  
  フォルダの PHP は **変更手順の履歴（Git と一緒に管理）**。DB の `migrations` テーブルは **どこまで実行したかのチェックリスト**で、**マイグレ 1 ファイル ≒ 1 行**なので行数は少ない。

- **セッション・キャッシュ・キュー**  
  **セッション** … ログイン状態などリクエスト間の「続き」。**キャッシュ** … 同じ取得・計算の再利用。**キュー** … 重い処理を後で回す待ち行列。`SESSION_DRIVER` 等で **DB に置く**と `sessions` / `cache` / `jobs` などのテーブルが使われる。考え方は Laravel 以外の RDB でも同じ。

- **実践（別リポ [CRUD](https://github.com/furuyaaaa/CRUD)）**  
  PostgreSQL 上で `ThousandDummyTasksSeeder`（`tasks` 1000 件）、`ThousandDummyUsersSeeder`（`users` 1000 件）でダミー投入。pgAdmin の Query Tool で `SELECT COUNT(*)`、複数文は **最後の結果だけ**表示されやすいので **1 文ずつ**確認するとよい。

### 今日の学習まとめ（Laravel 内部・SQL・設計）

- **PostgresGrammar（Laravel → PostgreSQL へ翻訳）**  
  `PostgresGrammar` は Laravel が組み立てたクエリを **PostgreSQL 向け SQL 文字列に変換する翻訳担当**。`whereBasic()` では **`like` / `ilike` 系で `::text` キャストを自動付与**、`whereBitwise()` では **PDO のプレースホルダと衝突しないよう `?` を `??` にエスケープ**することを学んだ。`compileInsertGetId()` で **PostgreSQL 専用の `RETURNING`** が付く仕組みも確認。

- **QueryBuilder — `select()` / `selectSub()`**  
  通常のカラムと **サブクエリ**を振り分ける仕組みを読んだ。**連想配列のキーが文字列かどうかで分岐する**ところがポイント。

- **Model.php — `boot()` まわり**  
  `bootIfNotBooted()` が **モデルを最初に使うときだけ一度だけ** boot を走らせること。`bootTraits()` が **`boot〇〇()` / `initialize〇〇()` をトレイトから自動検出して実行・登録**すること。`use SoftDeletes` の 1 行でソフトデリートが効く理由の一端を理解。

- **SQL（PostgreSQL／[PG Exercises](https://pgexercises.com/)）**  
  date・joins 系の演習のなかで **`EXTRACT(epoch FROM ...)`, `generate_series()`, タイムスタンプの引き算** などを学んだ。サブクエリが **SELECT / WHERE / FROM の 3 箇所に置ける**こと、特に **SELECT 句の相関サブクエリは行ごとに評価され JOIN より遅くなりやすい**ことを理解。**`INSERT` / `UPDATE` の基本と PostgreSQL の `RETURNING`** も整理。

- **SOLID**  
  単一責任・開放閉鎖・リスコフ・インターフェース分離・依存性逆転。**Grammar と QueryBuilder の責務分離や、DB 別 Grammar の拡張**に、読んだ Laravel コード上で繋がる実感が持てた。

- **気づき**  
  **QueryBuilder と Eloquent の違い**、**PDO と SQL インジェクション対策**、**`toSql()` でのデバッグ**など、実務につながる知識がまとまった。

## 2026-05-20

- **読んだファイル・方針**  
  `Illuminate\Auth\Access` 配下 **全ファイル** ＋ `PostgresGrammar.php`（途中）。**暗記ではなく「なぜ？」を追求する理解型学習。**

### 1. PHP の基礎

| キーワード | 内容 |
|------------|------|
| `ReflectionClass` | クラスの内部情報を読み取る PHP 組み込みクラス |
| クラスアトリビュート `#[...]` | PHP 8 からのメタデータをクラスに付与する仕組み |
| `::class` | クラスの完全修飾名を文字列で返す定数 |
| `public static` | インスタンス化不要で呼べる静的メソッド |
| `__construct` | インスタンス生成時に自動で呼ばれる特別なメソッド |
| `__toString` | オブジェクトを文字列として扱うとき自動で呼ばれる |
| クロージャ | 名前のない無名関数。変数に入れたり引数に渡せる |
| アロー関数 `fn() =>` | PHP 7.4 以降の短いクロージャ |
| スプレッド `...` | 配列を展開して引数として渡す |
| 型キャスト `(string)` | 値を指定の型に強制変換 |
| `new static` | 継承時に **子クラス** のインスタンスを生成 |
| `new self` | **定義したクラス** のインスタンスを生成（継承時は親） |

### 2. Laravel の知識

| キーワード | 内容 |
|------------|------|
| ポリシークラス | 「誰が・何を・できるか」の認可ロジックを 1 箇所に集約 |
| `Gate` クラス | 認可（権限チェック）を管理するオブジェクト（門番） |
| サービスコンテナ | 依存関係を自動解決してインスタンスを生成 |
| ファサード | コンテナ登録クラスへの窓口（静的メソッドに見える） |
| コレクション | 配列をより便利に操作する Laravel のクラス |
| `Arr::wrap()` | 値を必ず配列に変換するヘルパー |
| メソッドチェーン | `->` で複数メソッドを繋げるスタイル |
| DI（依存性注入） | 依存オブジェクトを外から注入する仕組み |
| ヘルパーメソッド | `Arr`・`Str` など、よく使う処理をまとめたもの |
| `forUser()` | 指定ユーザー用の新しい `Gate` インスタンスを返す |

### 3. 認可の仕組み（`Illuminate\Auth\Access`）

| キーワード | 内容 |
|------------|------|
| `before()` の優先実行 | ポリシーの全メソッドより **先** に実行される |
| `authorize()` | 拒否時に `AuthorizationException` を投げる |
| `AuthorizationException` | 認可エラーを表す例外 |
| `Response` クラス | 許可・拒否の結果を表す |
| `Response::allow()` / `deny()` | 許可／拒否レスポンスを生成 |
| `denyWithStatus()` | HTTP ステータス付き拒否 |
| `denyAsNotFound()` | 404 用の拒否レスポンス |
| 認証 vs 認可 | 認証＝「誰か？」、認可＝「何をしていいか？」 |

### 4. PHP 組み込み関数

| 関数 | 内容 |
|------|------|
| `class_exists()` / `method_exists()` | クラス認識・メソッド存在の確認 |
| `is_callable()` | 呼び出し可能か（`[オブジェクト, メソッド名]` も可） |
| `call_user_func()` | コールバック実行 |
| `explode()` / `implode()` | 文字列⇔配列の分割・結合 |
| `array_slice()` | 配列の一部を切り出し |
| `str_contains()` | 部分文字列の有無（PHP 8+） |
| `strtolower()` | 小文字化 |
| `sprintf()` | テンプレートに値を埋め込む |
| `dirname()` | パスからディレクトリ部分を取得 |
| `count()` | 配列の要素数 |

### 5. `PostgresGrammar.php`（読了: 途中）

| キーワード | 内容 |
|------------|------|
| `ILIKE` | 大文字小文字を区別しない検索（PostgreSQL） |
| `::text` キャスト | 型の不一致を防ぐ（`LIKE` 周りなど） |
| `CASCADE` + `TRUNCATE` | 関連テーブルも含めて TRUNCATE |
| `wrap()` | カラム名を `"` で囲み予約語衝突を防ぐ |
| `parameter()` | 値をプレースホルダーにし SQL インジェクション対策 |
| `$operators` | PostgreSQL で使える演算子を配列管理 |
| `sprintf()` で SQL 組み立て | `%s` 等で文字列テンプレート化 |
| `parent::` 委譲 | PG 固有でない処理は親クラスへ |
| `"` vs `'` | `"` は識別子（カラム・テーブル）、`'` は文字列リテラル |

### 6. 設計の知識

| キーワード | 内容 |
|------------|------|
| メタデータ | データについてのデータ（付加情報） |
| インターフェース | 「このメソッドを実装して」と約束する |
| `Arrayable` | `toArray()` を実装することを保証 |
| `Stringable` | `__toString()` を実装することを保証 |
| `extends` | 親の機能を引き継ぎ追加・上書き |
| ラッパーメソッド | 上の層が下の層を呼ぶ層構造 |
| 遅延評価 | 必要なタイミングで初めて実行 |
| ゲッター | プロパティを返すシンプルなメソッド |
| `toArray()` | オブジェクトを配列に変換 |

### 💡 今日の気づき

- **暗記ではなく理解** → **「なぜ？」を追求** → **Laravel の内部実装を読める力** がついてくる。  
- **SQL の基礎 ＋ ソースコードを読む ＝ 最強の組み合わせ**

### 📁 読んだファイル一覧

```text
vendor/laravel/framework/src/Illuminate/
├── Auth/
│   └── Access/
│       ├── AuthorizationException.php
│       ├── Gate.php
│       ├── HandlesAuthorization.php
│       └── Response.php
└── Database/
    └── Query/
        └── Grammars/
            └── PostgresGrammar.php（途中）
```

## 2026-05-19

- **Gate.php とは**  
  Laravel の **認可**（誰が何をできるか）を管理する門番クラス。`allows()` → `check()` → `inspect()` → `raw()` という順で処理が流れていく。

- **権限チェックの流れ**  
  - **`allows()` / `denies()`** … ラッパーメソッド。`check()` に丸投げするだけ。  
  - **`check()`** … `Collection` と `every()` で全 ability を確認。  
  - **`inspect()`** … 結果を `Response` オブジェクトに変換して返す。  
  - **`raw()`** … `before` → 本チェック → `after` の順で実行する核心部分。

- **今日覚えた構文・概念**  
  - **三項演算子 `? :`** … `if` / `else` を 1 行で書く。  
  - **`null` 合体演算子 `??`** … `null` のときの保険。  
  - **`instanceof`** … そのオブジェクトが指定クラスか判定。  
  - **アロー関数 `fn() =>`** … `return` も `use` も不要な短いクロージャ。  
  - **トレイト `use`** … クラスに貼り付けられる部品セット。  
  - **静的メソッド `::`** … オブジェクト不要でクラスから直接呼ぶ。  
  - **分割代入 `[$a, $b] = 配列`** … 配列を複数の変数に一気に代入。  
  - **`Collection`** … 配列の強化版。メソッドチェーンで操作しやすい。  
  - **`tap()`** … 値を返しながら別の処理も同時にやる。  
  - **`ReflectionClass`** … クラスの情報を動的に調べる PHP 組み込みクラス。  
  - **イベント発火** … 処理の完了を通知してリスナーに処理を委ねる仕組み。

- **気づき: ラッパーメソッドという設計**  
  `allows()` は `check()` に丸投げするだけなのに、なぜ存在するのか最初は疑問だった。でも **`Gate::allows('edit-post')` の方が英語として読みやすい**という理由がわかり、読みやすさのためにメソッドを分ける設計の意図が見えた。

- **気づき: コードは上から下に流れる（`return` と `catch` に注意）**  
  `try` の中は基本的に上から下に流れるが、**`return` が来たらそこで終了**、例外が起きたら **`catch` に飛ぶ**。この 2 つが「流れを変えるポイント」だと整理できた。

- **気づき: 命名規則でコードが読める**  
  **`resolve〇〇`** → オブジェクトを生成・取得 / **`call〇〇`** → 処理を実行 / **`get〇〇`** → 値を取得。メソッド名のパターンを知るだけで、中身を読まなくてもある程度意図がわかる。

## 2026-05-18

- **① クラス・インスタンス・プロパティ**  
  `class` は設計図、`new` で生成したものがインスタンス（実体）、インスタンスが持つ変数がプロパティ。`protected $gate;` はプロパティの宣言（箱を用意するだけで中身はまだ空）。変数の宣言と違い、値なしで宣言できる。

- **② コンストラクタと DI 注入**  
  `__construct()` はインスタンス生成時に自動で呼ばれる初期設定メソッド。引数にタイプヒンティング（`Gate $gate` など）を書くだけで、DI コンテナが自動でインスタンスを生成して渡してくれる。インスタンスを「作る」のは `new` キーワードであり、コンストラクタは「受け取って保存する場所」。

- **③ `$this` とプロパティへのアクセス**  
  `$this->gate` は「このインスタンス自身の `gate` プロパティ」を指す。`protected $gate` と `$this->gate` は同じプロパティを指しており、`$this->` をつけることで引数の `$gate` と区別できる。

- **④ アノテーション**  
  `@var`（プロパティの型）・`@param`（引数の型）・`@return`（戻り値の型）・`@throws`（投げる可能性のある例外）の 4 種類。PHP の動作には影響せず、IDE や開発者への説明コメント。

- **⑤ `static` メソッドとファサード**  
  `static` はインスタンスを作らずに `クラス名::メソッド名()` で呼べるメソッド。ファサードは DI コンテナのインスタンスに `::` で簡単アクセスする Laravel の特別な仕組みで、`Route`・`Gate`・`Auth`・`DB` など決まったクラスのみ。`PostController::class` の `::class` はメソッドではなく PHP のキーワードで、完全修飾クラス名を文字列で返すだけ。

- **⑥ Closure・callable・callback・`use`**  
  Closure は名前のない関数（無名関数）で変数に入れたり引数として渡せる。`callable` はそれを受け取る型、`callback` は後で呼び出すために渡す関数の使い方を指す言葉。`use` は外側の変数を Closure 内に引き継ぐキーワード。`fn($x) => $x` はアロー関数（短い書き方）。

- **⑦ `Authorize.php` の仕組み**  
  ミドルウェアとして動作し、リクエストが来たときに `$this->gate->authorize()` で権限チェックをしてから `$next($request)` で次の処理に渡す門番の役割。`using()` は `クラス名:ability名,モデル名` という設定文字列を生成する `static` メソッド。

- **⑧ ability と Gate の権限管理**  
  ability は「誰が何をできるか」を表す権限の名前。`Gate::define()` で登録し、`Gate::check()` / `Gate::authorize()` で確認する。`Gate::resource()` を使うと `viewAny`・`view`・`create`・`update`・`delete` の 5 つをまとめて定義できる。

- **⑨ Enum（PHP 8.1+）**  
  決まった値の集まりをまとめる仕組み。値なし（`UnitEnum`）と値あり（`BackedEnum`）の 2 種類がある。文字列をそのまま使うよりタイポを防げる。`enum_value()` を使うと Enum でも文字列でも統一して文字列として取得できる。

- **⑩ ルーティングと CRUD**  
  `Route::get/post/put/delete()` の第一引数が URL、第二引数が `[コントローラー::class, 'メソッド名']` の配列。`Route::resource()` で 7 つのルートをまとめて登録できる。コントローラーの引数に `Request $request` と書くだけで DI コンテナが自動注入してくれる。

- **⑪ Laravel と PostgreSQL**  
  [CRUD](https://github.com/furuyaaaa/CRUD) の接続を `.env` の `DB_CONNECTION=pgsql` とホスト・ポート・DB 名・ユーザー／パスワードで Postgres に向ける。サーバ側で DB を作ってから `php artisan migrate`。`INFO Nothing to migrate.` はエラーではなく「未実行のマイグレがない」状態。`php artisan db:show` が `intl` で止まる場合は PHP の `extension=intl` を有効にする（接続とは別）。

- **⑫ シーダー（DatabaseSeeder）**  
  `php artisan db:seed` で `users` / `tasks` に初期データを流し込める。`User::firstOrCreate` で決まったメールのユーザーを用意し、`User::factory()` で人数を増やす。`User::factory()->make()->toArray()` だけだと `password` が `$hidden` で配列に乗らず NOT NULL 違反になるので、`firstOrCreate` 第 2 引数では `password` を明示（`User` の `hashed` キャストでハッシュ化）。`Task::factory()->for($user)` で `user_id` を付けたタスクを作る。

- **⑬ pgAdmin 4**  
  データベース `crud` → `Schemas` → `public` → `Tables` に `users`・`tasks`。Query Tool は **SQL エディタと Data Output の境界線を上にドラッグ**すると入力欄が現れる。実行は ▶ または **F5**。

- **⑭ SQL で特定の行を取り出す**  
  `SELECT 列 FROM 表 WHERE 条件`。`tasks.user_id` は `users.id` を参照するので、`JOIN users u ON u.id = t.user_id` でメールや名前付きでタスク一覧を取れる。`GROUP BY` と `COUNT` でユーザーごとの件数集計もできる。

- **⑮（振り返り）PHP 公式ドキュメント**  
  PHP 公式ドキュメントを読む。「公式一次情報を読む大切さ」を理解。（時系列の詳細は下記 **付記** の 2026-05-18 も参照）

## 2026-05-15

- **① DIコンテナ**  
  必要なクラスを `ServiceProvider` で登録しておくことで、コンストラクタに型を書くだけで自動でインスタンスを作って注入してくれる仕組み。

- **② ServiceProvider**  
  Laravel が起動するときに DI コンテナにサービスを登録する場所。`bind()` は毎回新しいインスタンスを作り、`singleton()` は 1 つだけ作って使い回す。

- **③ Closure オブジェクト**  
  無名関数をオブジェクトとして扱えるようにしたもの。変数に入れたり引数に渡したり戻り値として返せる「持ち運びできる関数」。

- **④ `Pipeline.php` の仕組み**  
  `send()`・`through()`・`via()`・`then()` をメソッドチェーンで繋げて、リクエストをミドルウェアに順番に通し、最終的にコントローラーへ届ける仕組み。

- **⑤ メソッドチェーン**  
  `return $this` で自分自身を返すことで、メソッドを数珠つなぎに呼び出せる仕組み。`@return $this` で見分けられる。

- **⑥ `$next` の正体**  
  `Pipeline.php` の `carry()` で組み立てたクロージャが、ミドルウェア（例: `Authorize.php`）の `handle()` に渡される `$next` と対応しており、引数の順番が一致するため同じ流れとして繋がる。

## 2026-05-14

- **やったこと**: プロフィール用リポジトリをローカルにクローンし、学習ログ用ファイルを用意した
- **理解したこと**: プロフィール README と日記を分けると運用しやすい
- **次やること**（任意）:
- **（振り返り・5月中旬）** README・ポートフォリオ構想を本格化。「2 月からの学習実績を形にしたい」と考える（時系列は下記 **付記** の 2026-05-14 も参照）

---

## 付記：転職・学習の記録（2026-02-02 〜 2026-05-18）

以下は **時系列（古い順）**。営業からエンジニアを目指して学習を始めてからの振り返りメモ。上の「日付ごとのメモ」とあわせて読む用。

### 2026-02-02

- 「営業だけでは今後厳しい」と感じ始める
- SQL・IT資格・エンジニア転職を真剣に検討
- “自分の力で稼げるスキル”への意識が強くなる

### 2026-02-04

- PostgreSQL 学習開始
- `SELECT` 文や `WHERE` 句を触り始める
- 「DB って裏側こうなってるのか」と興味が強くなる

### 2026-02-06

- SQL のエラーに苦戦
- `GROUP BY` / `ORDER BY` の違いを学習
- エラー文を読む習慣が少しずつつく

### 2026-02-08

- `JOIN` 句を学習
- 「テーブル同士を繋ぐ」という概念を理解
- 業務システムの裏側イメージが見え始める

### 2026-02-10

- PostgreSQL 環境構築
- pgAdmin や設定ファイルに触れる
- 「conf」「postgresql.conf」など運用知識に興味

### 2026-02-12

- Git / GitHub を使い始める
- CLI 操作に慣れ始める
- 「黒い画面怖い」が少し減る

### 2026-02-14

- Laravel / PHP 学習開始
- Middleware や DI（依存性注入）に触れる
- オブジェクト指向の考え方に混乱

### 2026-02-16

- `Gate` クラスや認証周りを学習
- 「インスタンス」「`protected`」の意味を調べる
- コードを読める感覚が少し出てくる

### 2026-02-18

- SQL 問題を大量に解く
- `UPDATE` / `INSERT` / `DELETE` を学習
- 「主キーを使う理由」が理解でき始める

### 2026-02-20

- Linux・CLI 操作に慣れてくる
- 「エンジニアっぽいことやってる感覚」が出始める
- 小さな成功体験が増える

### 2026-02-22

- OSS-DB 系の学習を本格化
- DB 設計や正規化に触れる
- インフラ寄り知識にも興味が広がる

### 2026-02-24

- RAID、CPU、メモリなどハードウェア学習
- IT 基礎力を強化
- 「営業時代には触れなかった世界」を実感

### 2026-02-26

- SQL 集計問題を繰り返す
- `SUM`、`COUNT`、`GROUP BY` に慣れる
- “考えて書く”感覚が身につき始める

### 2026-02-28

- GitHub に学習記録を残そうと考え始める
- README やポートフォリオ意識が芽生える
- 「積み上げを見える化したい」と感じる

### 2026年3月

### 2026-03-02

- エンジニア転職への意識がさらに強くなる
- 「営業経験も武器になる」と考え始める

### 2026-03-04

- Laravel 構成理解が少し進む
- `namespace` / `use` / `Closure` を学習
- 「PHP って意外と奥深い」と感じる

### 2026-03-06

- PostgreSQL 認証やパスワード周りに苦戦
- `.env` ファイルや接続設定を理解
- “実務っぽいトラブル対応”を経験

### 2026-03-08

- SQL 問題演習量が増える
- サブクエリ理解に挑戦
- 「頭使うけど面白い」に変化

### 2026-03-10

- GitHub CLI 認証
- Token や認証概念を学ぶ
- セキュリティ意識が出始める

### 2026-03-12

- テスト（Unit Test / Feature Test）学習
- スタブ・ドライバ概念を理解
- 「品質保証」の考え方に触れる

### 2026-03-14

- AWS や Snowflake にも興味
- データエンジニア路線を意識し始める

### 2026-03-16

- 「年収を上げるには技術力」と確信
- データエンジニア戦略を考え始める

### 2026-03-18

- SQL エラー耐性がかなり上がる
- 英文問題も読み慣れてくる
- 技術英語への抵抗が減少

### 2026-03-20

- README 作成構想
- 学習ログを資産化したいと考える
- 「転職市場で戦う準備」を意識

### 2026年4月

### 2026-04-02

- Laravel 構造理解が進む
- 認証・イベント・Middleware 周辺に慣れる

### 2026-04-06

- PostgreSQL 設定理解が深まる
- レプリケーションや接続概念を学習

### 2026-04-10

- OSS-DB 問題演習を継続
- DB 内部理解がかなり進む

### 2026-04-14

- 「営業→エンジニア転身」の覚悟が固まる
- 将来的な独立や収益化も視野に入る

### 2026-04-18

- GitHub を“実績置き場”として意識
- 学習内容を整理したくなる

### 2026-04-22

- HTML / CSS にも触れ始める
- フロント側にも興味を持つ
- 「AI に書かせながら理解する」発想が出始める

### 2026-04-26

- キャリア戦略を真剣に考える
- 「今は積み上げる時期」と認識

### 2026-04-30

- 2 月からの継続学習量を振り返る
- 「かなり変わった」と実感
- エンジニアとしての自己認識が芽生え始める

### 2026-05-01

- 学習を“点”ではなく“キャリア”として考え始める
- 「自分の市場価値を上げる」という視点が強くなる
- GitHub・資格・実務・ポートフォリオを繋げる意識が出てくる

### 2026-05-06

- Laravel 認証系コードを読む
- `Gate`・`Authenticated` イベントなどを確認
- 「フレームワーク内部」を読む習慣がつく

### 2026-05-08

- PostgreSQL 設定ファイルを学習
- `pg_hba.conf` / `postgresql.conf` 理解
- “実務運用寄り”の知識が増える

### 2026-05-10

- GitHub CLI や認証周りに慣れる
- Token 概念や権限管理を理解

### 2026-05-12

- SQL 英文問題への耐性向上
- 「英語を読む → SQL を書く」の流れが安定

### 2026-05-14

- README・ポートフォリオ構想を本格化
- 「2 月からの学習実績を形にしたい」と考える

### 2026-05-16

- データエンジニア路線を強く意識
- Python / Snowflake / AWS への興味拡大
- 「年収を上げるには市場価値」と考え始める

### 2026-05-17

- “恐怖＝成長前のサイン”という考え方に共感
- キャリアチェンジへの不安と覚悟を整理
- 「逃げずに積み上げる」と決意を強める

### 2026-05-18

- PHP 公式ドキュメントを読む
- 「公式一次情報を読む大切さ」を理解
